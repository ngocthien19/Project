import tkinter as tk
from tkinter import font

def ChartPage(root):
    global chartPage
    chartPage = tk.Toplevel()
    chartPage.title("Chart")
    chartPage.geometry("1200x600")
    chartPage.config(bg="#FFF")